
spring:
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
  datasource:
    url: jdbc:mysql://${DB_HOST:localhost}:3306/employeedb?createDatabaseIfNotExist=true&useSSL=true
    password: root
    username: root
    driver-class-name: com.mysql.cj.jdbc.Driver
  application:
    name: EMPLOYEE-SERVICE

